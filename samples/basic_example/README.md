# Basic example

This example deploys a pod (nginx) and tests if the webserver is running.

Please make sure to use a proper `partner_creds` value, pointing to a file that contains the credentials to access to Docker registry.
