apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fake-cnf.fullname" . }}-test-connection"
  labels:
    {{- include "fake-cnf.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fake-cnf.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
