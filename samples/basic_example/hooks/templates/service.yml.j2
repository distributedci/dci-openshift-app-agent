apiVersion: v1
kind: Service
metadata:
  app: httpd
  name: webserver
spec:
  ports:
  - name: webserver1
    port: 8080
    protocol: TCP
    targetPort: 80
  type: NodePort
  ipFamilyPolicy: PreferDualStack
  selector:
    app: httpd
