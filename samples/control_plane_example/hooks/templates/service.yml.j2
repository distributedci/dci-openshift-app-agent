apiVersion: v1
kind: Service
metadata:
  app: httpbin
  name: httpbin
spec:
  ports:
  - name: httpbin
    port: 8080
    protocol: TCP
    targetPort: 8080
  type: NodePort
  ipFamilyPolicy: PreferDualStack
  selector:
    app: httpbin
